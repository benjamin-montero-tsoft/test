<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://java.sun.com/xml/ns/j2ee" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://java.sun.com/xml/ns/j2ee http://java.sun.com/xml/ns/j2ee/web-app_2_4.xsd"
	version="2.4">

	<listener>
		<listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
	</listener>
	<listener>
		<listener-class>org.springframework.security.web.session.HttpSessionEventPublisher</listener-class>
	</listener>
	
	<context-param>
		<param-name>contextConfigLocation</param-name>
		<param-value>classpath:applicationContext.xml classpath:spring-security.xml</param-value>
	</context-param>

	<context-param>
		<param-name>log4jConfigLocation</param-name>
		<param-value>classpath:log4j.xml</param-value>
	</context-param>
	<!--JMesa es una tabla de HTML dinámica que le permite filtrar, ordenar, paginar, exportar y editar 
	sus datos. Lo que usted encontrará aquí es una API cuidadosamente pensada que es muy fácil de usar y
	 puede ser modificado para sus propias necesidades.-->
	 <!-- “jmesaMessagesLocation” se utiliza para recuperar todo el texto de configuración regional específica.
	 La implementación por defecto usa paquetes de recursos Java. Para configurar el paquete de recursos especificar
	 un context-param en el archivo / WEB-INF/web.xml.-->	 
	<context-param>
  		<param-name>jmesaMessagesLocation</param-name>
  		<param-value>messages</param-value>
	</context-param>
	<!-- “jmesaPreferencesLocation” se utiliza para evitar tener que codificar los valores de los atributos por
	defecto que utiliza JMesa. La implementación por defecto utiliza un archivo de propiedades Java. 
	Si se necesita reemplazar alguna de estas opciones por defecto, se realiza mediante la creación de un propio
	archivo jmesa.properties y luego se reemplaza los valores que se desee -->
	<context-param>
		<param-name>jmesaPreferencesLocation</param-name>
		<param-value>/jmesa.properties</param-value>
	</context-param>	
	<!--servlet-name: Nombre del servlet. Servlet-class: Clase del servlet.
	<load-on-startup> indica que el servlet asociado se debe cargar e iniciar cuando se inicie la aplicación web de la que forma parte.
	El contenido opcional de este elemento es un entero que indica el orden en el que se debe cargar e iniciar el servlet con respecto a
	los demás servlets de la aplicación web. Si <load-on-startup> está vacío, indica que el orden no es relevante, siempre y cuando el
	servlet se cargue e inicie durante el inicio de la aplicación web que lo contiene.-->		
	<!-- Lo siguiente inicializa el contexto spring MVC mediante un init-param (el contexto spring de MVC que ya definimos arriba se inicializa con el
	 archivo dispatchContext.xml):-->	
	<servlet>
		<servlet-name>dispatcher</servlet-name>
		<servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
		<init-param>
			<param-name>contextConfigLocation</param-name>
			<param-value>classpath:dispatchContext.xml</param-value>
		</init-param>
		<load-on-startup>1</load-on-startup>
	</servlet>

	<servlet-mapping>
		<servlet-name>dispatcher</servlet-name>
		<url-pattern>*.do</url-pattern>
	</servlet-mapping>
	
	<servlet>
		<servlet-name>Resource Servlet</servlet-name>
		<servlet-class>org.springframework.js.resource.ResourceServlet</servlet-class>
	</servlet>

	<servlet-mapping>
		<servlet-name>Resource Servlet</servlet-name>
		<url-pattern>/resources/*</url-pattern>
	</servlet-mapping>
	<!-- Mantener orden de filtros -->
	<!--"securityContextPopulatorFilter“ es el primer filtro. En el “applicationContext.xml” 
	se redefinio la clase del filtro a “SecurityContextPopulatorFilter.java”-->	
	<filter>
        <filter-name>securityContextPopulatorFilter</filter-name>
        <filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
    </filter>
    <filter-mapping>
      <filter-name>securityContextPopulatorFilter</filter-name>
      <url-pattern>/*</url-pattern>
    </filter-mapping>
    <!-- El filtro “springSecurityFilterChain” se define para interceptar todas las peticiones.
    Este filtro escuchará las solicitudes de login/loguot de sesión y procesará en consecuencia.
    También capturará AccesDeniedExceptions y redirigir al usuario a la página de inicio de sesión.-->    
	<filter>
        <filter-name>springSecurityFilterChain</filter-name>
        <filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
    </filter>
    <filter-mapping>
      <filter-name>springSecurityFilterChain</filter-name>
      <url-pattern>/*</url-pattern>
    </filter-mapping>

</web-app>

